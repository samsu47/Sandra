<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Kitty Valentine</title>
    <link href="https://fonts.googleapis.com/css2?family=Loved+by+the+King&family=Itim&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #ffc0cb;
            text-align: center;
            font-family: 'Itim', cursive;
            margin: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            touch-action: manipulation;
        }

        .container {
            position: relative;
            padding: 20px;
            max-width: 90%;
        }

        .cat-img {
            width: 80%;
            max-width: 250px;
            height: auto;
            margin-bottom: 15px;
        }

        h1 {
            font-family: 'Loved by the King', cursive;
            font-size: 2em;
            color: #ff3366;
            margin-bottom: 20px;
            line-height: 1.3;
        }

        .buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
        }

        button {
            padding: 12px 25px;
            font-size: 1.1em;
            border: none;
            border-radius: 15px;
            cursor: pointer;
            transition: transform 0.3s;
            min-width: 120px;
            touch-action: manipulation;
        }

        #yesBtn {
            background-color: #ff69b4;
            color: white;
        }

        #noBtn {
            background-color: #cccccc;
            color: #666666;
            cursor: not-allowed;
        }

        #message {
            display: none;
            font-size: 1.5em;
            color: #ff3366;
            margin-top: 20px;
            padding: 0 15px;
        }

        .balloon {
            position: absolute;
            width: 40px;
            height: 50px;
            background-color: #ff69b4;
            border-radius: 50%;
            animation: float 4s ease-in infinite;
        }

        .balloon::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 20px;
            width: 2px;
            height: 30px;
            background-color: #333;
        }

        .kiss {
            position: absolute;
            font-size: 25px;
            animation: kiss 2s linear;
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 1.8em;
            }
            
            button {
                padding: 10px 20px;
                font-size: 1em;
                min-width: 100px;
            }
            
            .buttons {
                gap: 10px;
                flex-direction: column;
            }
            
            #message {
                font-size: 1.3em;
            }
        }

        @keyframes float {
            0% { transform: translateY(100vh); }
            100% { transform: translateY(-100vh); }
        }

        @keyframes kiss {
            0% { opacity: 1; transform: translateY(0); }
            100% { opacity: 0; transform: translateY(-100px); }
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="https://i.imgur.com/r7oM7Wq.png" alt="Cute Cat" class="cat-img">
        <h1>Will you be my Valentine? 😽</h1>
        <div class="buttons">
            <button id="yesBtn">YES!</button>
            <button id="noBtn" disabled>NO</button>
        </div>
        <div id="message">Happy Valentine's Day! 💖😽 Meow!</div>
    </div>

    <script>
        const yesBtn = document.getElementById('yesBtn');
        const message = document.getElementById('message');
        const container = document.querySelector('.container');

        yesBtn.addEventListener('click', () => {
            document.querySelector('.buttons').style.display = 'none';
            message.style.display = 'block';

            // Mobile-friendly animations
            const isMobile = /iPhone|iPad|iPod|Android/i.test(navigator.userAgent);
            const balloonCount = isMobile ? 15 : 20;
            
            for (let i = 0; i < balloonCount; i++) {
                createBalloon();
            }

            const kisses = ['💖', '💋', '😽', '💘', '🥰'];
            const kissInterval = isMobile ? 400 : 300;
            
            const kissTimer = setInterval(() => {
                const kiss = document.createElement('div');
                kiss.className = 'kiss';
                kiss.textContent = kisses[Math.floor(Math.random() * kisses.length)];
                kiss.style.left = Math.random() * 100 + 'vw';
                container.appendChild(kiss);
                
                setTimeout(() => kiss.remove(), 2000);
            }, kissInterval);

            // Cleanup after 10 seconds
            setTimeout(() => {
                clearInterval(kissTimer);
                document.querySelectorAll('.balloon, .kiss').forEach(el => el.remove());
            }, 10000);
        });

        function createBalloon() {
            const balloon = document.createElement('div');
            balloon.className = 'balloon';
            balloon.style.left = Math.random() * 100 + 'vw';
            balloon.style.backgroundColor = `hsl(${Math.random() * 360}, 70%, 60%)`;
            balloon.style.animationDuration = Math.random() * 3 + 3 + 's';
            document.body.appendChild(balloon);

            setTimeout(() => balloon.remove(), 5000);
        }
    </script>
</body>
</html>
